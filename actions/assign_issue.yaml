ref: jira.assign_issue
label: "Assign Jira Issue"
description: "Assign a Jira issue to a user"
enabled: true
runner_type: python
runtime_version: ">=3.10"
entry_point: jira_action.py
parameter_delivery: stdin
parameter_format: json
output_format: json
default_execution_permission_set_refs: [standard]
parameters:
  credential_key: {type: string, default: "jira.credentials", description: "Pack-owned Jira credential Key ref"}
  issue: {type: string, required: true}
  assignee: {type: string, required: true}
output:
  operation: {type: string, required: true}
  result: {type: object, description: "Assignment status and Jira response"}
tags: [jira, issue, write]
